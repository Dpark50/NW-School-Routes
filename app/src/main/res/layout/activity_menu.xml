<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_menu"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="dtsquared.nwschoolsafewalk.Menu">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="200dp"
        app:srcCompat="@drawable/walklogo"
        android:id="@+id/image_newwest"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="50dp"
        android:contentDescription="@string/logo"/>

    <ImageButton
        android:id="@+id/map_button"
        android:layout_width="270dp"
        android:layout_height="60dp"
        android:background="@drawable/btnmap"
        android:onClick="map"
        android:scaleType="fitCenter"
        android:layout_below="@+id/image_newwest"
        android:layout_alignStart="@+id/account_button"
        android:contentDescription="@string/mapbutton"/>

    <ImageButton
        android:layout_width="270dp"
        android:layout_height="60dp"
        android:id="@+id/account_button"
        android:background="@drawable/btnacct"
        android:onClick="accountInformation"
        android:scaleType="fitCenter"
        android:contentDescription="@string/accountbutton"
        android:layout_below="@+id/map_button"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="32dp" />

    <ImageButton
        android:layout_width="270dp"
        android:layout_height="60dp"
        android:id="@+id/route_button"
        android:background="@drawable/btnroute"
        android:onClick="route"
        android:scaleType="fitCenter"
        android:contentDescription="@string/routebutton"
        android:layout_alignStart="@+id/account_button"
        android:layout_below="@id/account_button"
        android:layout_marginTop="32dp" />
</RelativeLayout>
